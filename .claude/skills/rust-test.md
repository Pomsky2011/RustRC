# Rust Test

Run tests with detailed output.

## Steps
1. Run `cargo test -- --nocapture` to run all tests with stdout visible
2. If a specific test name is provided as an argument, run `cargo test <name> -- --nocapture` instead
3. Summarize pass/fail results
